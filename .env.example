# Redis — see CLAUDE.md "Environment" section.
# docker run -p 6379:6379 redis:7-alpine
REDIS_URL=redis://localhost:6379/0

# Consumer group / stream names (CLAUDE.md "Why Redis Streams specifically")
TRADES_STREAM=trades
WORKER_GROUP=tieout-workers

LOG_LEVEL=INFO
